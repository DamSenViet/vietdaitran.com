import React from 'react'
import { Box } from '@mui/material'

import { useRouter } from 'next/router'

// goes inside the layout or is a layout itself
export default function SplashScreen() {
  // React.useEffect(() => {}, [])
  // after first mount, trigger animation

  return <>{/* wow */}</>
}
